全局变量 logo[],下标
	backpicture[],下标
	colorchange[],下标,
	日期、时间
	颜色、、、



header -> logo     usb  net  sd  date time

wim_main ->调色->返回
	->背景色、背景图片、标题logo->确认、返回
	->串口   设置、换行、清空、返回
	->软件图标->返回
	->日期时间设置->确认、返回
	->密码->选框、小键盘->进入->字体显示，返回
			->返回
	->时针转动，进度条->返回
	->网络->小键盘设置
	->表格->删减、增加，进度条开关、返回
	->二维码显示